describe('#scale()', function() {
    it('should scale in', function() {
        var cropbox = new Cropbox('#plugin');
    });
    it('should scale out', function() {
        var cropbox = new Cropbox('#plugin');
    });
});
